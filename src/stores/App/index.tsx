import stores_profile from "#src/stores/profile";

const state = {};

const actions = {
  fetch: async () => {
    // setState
    return state;
  },
};

export const stores_App = {
  actions,
  state,
};

export default stores_App;
